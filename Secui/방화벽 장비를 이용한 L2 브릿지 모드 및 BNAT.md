### L2 브릿지 구성
- 방화벽의 인터페이스를 L2(브릿지) 모드로 전환
- 전환한 인터페이스를 br0(브릿지)로 묶기
- 브릿지(방화벽)의 관리 IP를 설정

```

```
### BNAT
- 스위치 역할을 하면서 관리 IP를 통해 NAT(Network Address Translation)을 수행
- 호스트의 기본 게이트웨이 주소를 브릿지의 관리 IP로 설정
- 본래는 라우팅 기반으로 동작하는 L3 기능인 NAT를 브릿지를 이용해 수행함
- 같은 네트워크 내에서도 주소를 번역해 보여주는 기능